import closetController from "../Controllers/closetController";
describe("create user", () => {
  test("all cases hit correctly", () => {});
  test("no bananss", () => {});
  test("User already exits", () => {});
  test("When user is created, are all the fields assigned correctly", () => {});
  test("is an id created?", () => {});
  test("was the default (all gear) closet created?, no bananss", () => {});
});
